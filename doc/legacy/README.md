# Documentação Legada

Este diretório preserva artefatos antigos que não devem mais ser usados como referência principal do projeto.

Estado atual:

- `cockburn_project.md` foi substituído por [doc/cockburn/cockburn.md](../cockburn/cockburn.md).
- `use-cases/` contém diagramas `.drawio` com numeração e recortes anteriores ao alinhamento atual das features.

Regra de uso:

- consultar apenas para contexto histórico;
- não usar estes arquivos como base canônica para novas features, regras de negócio ou implementação.
